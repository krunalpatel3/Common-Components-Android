<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="krunal.com.example.commoncomponents">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SeekbarActivity"
            android:label="@string/seekbar" />
        <activity
            android:name=".ToggleButtonActivity"
            android:label="@string/Togglebutton" />
        <activity
            android:name=".CheckBoxandRadioButtonActivity"
            android:label="@string/checkbox_and_radiobutton" />
        <activity
            android:name=".SwitchActivity"
            android:label="@string/switch1" />
        <activity
            android:name=".ImageButtonActivity"
            android:label="@string/imagebutton" />
        <activity android:name=".SnackbarActivity"
            android:label="@string/SnakeBar"></activity>
    </application>

</manifest>